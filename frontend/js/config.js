/* Configuration du front statique (Vercel).
   API_BASE = URL publique HTTPS du backend Flask (tunnel Cloudflare du VPS).
   Exemple : "https://scanner.mondomaine.com"  — SANS slash final. */
const API_BASE = "https://sympathy-recently-roles-cellular.trycloudflare.com";
